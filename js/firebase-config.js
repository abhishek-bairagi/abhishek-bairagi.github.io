/**
 * Replace with your Firebase web app config (Project settings → Your apps).
 * This object is safe to commit: security is enforced by Firestore rules, not secrecy of these fields.
 */
export const firebaseConfig = {

};